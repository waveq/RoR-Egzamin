$('#gists-list').append('<%= j render @gists %>');

<% if @gists.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate @gists %>');
<% else %>
  $(window).off('scroll');
  $('.pagination').remove();
<% end %>
